/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "i.ibb.co",
          "localhost:3000",
          "example.com",
          "cloudfront.net",
          "via.placeholder.com"
        ],
      },
};

export default nextConfig;


